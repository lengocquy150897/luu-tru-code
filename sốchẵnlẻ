# luu-tru-code
lưu lại những gì đã học code
// nhap mot so nguyen duong n. 
// cho biet n la so chan hay so le.

#include <stdio.h>

main() {
	int n;
	printf("Nhap mot so nguyen duong: ");
	scanf("%d", &n);
	
	if (n%2==0)
		printf(" %d la so chan", n);
	else
		printf(" %d la so le", n);
}
